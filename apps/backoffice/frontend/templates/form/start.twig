<form id="{{ id|default }}" name="{{ name|default }}" action="{{ action|default }}" method="{{ method }}"
    {% if multipart %} enctype="multipart/form-data"{% endif %}
    {% if classes %} class="{{ classes }}"{% endif %}
>
